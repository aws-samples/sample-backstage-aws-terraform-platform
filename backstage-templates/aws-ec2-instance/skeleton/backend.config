# Terraform backend configuration for EC2 instance: ${{ values.instanceName }}
# This file is auto-generated by Backstage

bucket  = "YOUR_TERRAFORM_STATE_BUCKET"
key     = "backstage-poc/ec2/${{ values.instanceName }}/${{ values.environment }}/terraform.tfstate"
region  = "YOUR_AWS_REGION"
encrypt = true

# State locking with DynamoDB (recommended for production)
# Uncomment the line below and create the DynamoDB table for state locking
# dynamodb_table = "terraform-state-lock"
